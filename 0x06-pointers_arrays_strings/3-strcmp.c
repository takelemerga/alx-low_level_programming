#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: string 1
 * @s2: string 2
 * Return: 1 if true, 0 if false
 */
int _strcmp(char *s1, char *s2)
{
	int diff;

	diff = *s1 - *s2;
	if (diff != 0)
	{
	diff = *s1 - *s2;
	return (diff);
	}
	return (diff);
}
